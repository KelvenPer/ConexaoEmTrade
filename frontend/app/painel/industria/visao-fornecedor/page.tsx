export default function VisaoFornecedorPage() {
  return (
    <div>
      <h1 style={{ fontSize: 20, fontWeight: 700, marginBottom: 8 }}>
        Visão por Fornecedor
      </h1>
      <p style={{ fontSize: 13, color: "#6b7280", maxWidth: 520 }}>
        Painel focado em cada fornecedor, usando aquela tabela de fornecedores
        que você já criou no backend.
      </p>
    </div>
  );
}
